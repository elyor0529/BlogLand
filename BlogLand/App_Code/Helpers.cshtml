@helper  Tags(System.Web.Mvc.HtmlHelper htmlHelper,
    IList<BlogLand.Models.Tag> tags)
{
foreach (var tag in tags)
{
        <span>
            @BlogLand.ExtensionLinks.TagLink(htmlHelper, tag)
        </span>
}
}